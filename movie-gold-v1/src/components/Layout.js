import { Outlet } from "react-router-dom";
// rafce to get skeleton code
import React from "react";

const Layout = () => {
  return (
    <main>
      <Outlet />
    </main>
  );
};

export default Layout;
